{
  "name": "demo-app",
  "version": "1.0.0",
  "description": "A simple demo app listening on port 80",
  "main": "app.js",
  "scripts": {
    "start": "node dist/app.js",  
    "build": "mkdir -p dist && cp app.js package.json dist/ && npm install --prefix dist"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "author": "",
  "license": "ISC"
}
